SET(OUT_NAME TestIO)
AUX_SOURCE_DIRECTORY(. SOURCE_LIST)

INCLUDE_DIRECTORIES(
 ${CVL_INCLUDE_DIR}
)

LINK_DIRECTORIES(
 ${CVL_LIB_OUT_DIR}
)

ADD_EXECUTABLE(${OUT_NAME} ${SOURCE_LIST})
		
SET_TARGET_PROPERTIES(${OUT_NAME} PROPERTIES FOLDER	Test)
